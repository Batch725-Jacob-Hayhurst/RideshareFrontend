import { UsernameAvailabilityDirective } from './username-availability.directive';

describe('UsernameAvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameAvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
