import { UsernameCheckDirective } from './username-check.directive';

describe('UsernameCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameCheckDirective();
    expect(directive).toBeTruthy();
  });
});
